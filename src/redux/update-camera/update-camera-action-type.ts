import { FeatureKeys } from '../featureKeys';

type UpdateCameraTypes = {
	UPDATE_CAMERA_FETCH: string;
};

export default <UpdateCameraTypes>{
	UPDATE_CAMERA_FETCH: `${FeatureKeys.UPDATE_CAMERA}/FETCH`,
};
