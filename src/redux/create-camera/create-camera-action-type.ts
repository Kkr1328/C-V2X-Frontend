import { FeatureKeys } from '../featureKeys';

type CreateCameraTypes = {
	CREATE_CAMERA_FETCH: string;
};

export default <CreateCameraTypes>{
	CREATE_CAMERA_FETCH: `${FeatureKeys.CREATE_CAMERA}/FETCH`,
};
