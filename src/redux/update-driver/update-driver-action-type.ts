import { FeatureKeys } from '../featureKeys';

type UpdateDriverTypes = {
	UPDATE_DRIVER_FETCH: string;
};

export default <UpdateDriverTypes>{
	UPDATE_DRIVER_FETCH: `${FeatureKeys.UPDATE_DRIVER}/FETCH`,
};
