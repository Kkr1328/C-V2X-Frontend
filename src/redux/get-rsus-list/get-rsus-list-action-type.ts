import { FeatureKeys } from '../featureKeys';

type GetRSUsListTypes = {
	GET_RSUS_LIST_FETCH: string;
};

export default <GetRSUsListTypes>{
	GET_RSUS_LIST_FETCH: `${FeatureKeys.GET_RSUS_LIST}/FETCH`,
};
